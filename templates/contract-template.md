# 契約: [インターフェース名]

> **段階**: ④ Plan の補助（`plan.md` §4 の詳細）
> **対応する設計**: `./plan.md`  |  **対応する仕様**: `./spec.md`
> **作成日**: [YYYY-MM-DD]
>
> 外部との境界（API・コマンド・公開関数）の **約束事** を、呼び出す側・実装する側の
> 双方が依存できる形で固定する。境界が複数あるなら `contracts/` 配下に分けて置く
> （例: `contracts/create-user.md`）。言語非依存——具体的な型は plan の技術スタックに従う。

---

## 1. 概要

- **種別**: [HTTP API / CLI コマンド / 関数・メソッド / メッセージ]
- **責務**: [この境界が何を引き受けるか（1〜2文）]
- **対応する仕様**: [US-x / AC-x.x — この契約がどの受け入れ基準を支えるか]

## 2. シグネチャ / エンドポイント

> 呼び出しの形を1つに固定する。

```
[例: POST /api/users  |  cmd: new-user --name <str>  |  fn: createUser(input) -> Result]
```

## 3. 入力

| 名前 | 型 | 必須 | 制約 | 説明 |
|------|----|----|------|------|
| [...] | [...] | ✓ | [...] | [...] |

## 4. 出力（正常系）

| フィールド | 型 | 説明 |
|------------|----|------|
| [...] | [...] | [...] |

- **成功の判定**: [例 — HTTP 201 / 終了コード 0 / Ok(value)]

## 5. エラー（異常系）

> 起こりうる失敗を列挙する。沈黙のエラーを残さない。

| 条件 | 返し方 | 呼び出し側の対処 |
|------|--------|------------------|
| [入力不正] | [400 / 終了コード2 / Err(...)] | [...] |
| [...] | [...] | [...] |

## 6. 事前条件 / 事後条件

- **事前条件**: [呼び出す前に成立していなければならないこと]
- **事後条件**: [正常終了後に保証されること]
- **冪等性**: [同じ呼び出しを繰り返したときの挙動。あれば]

> これらは契約テスト（contract test）の直接の根拠になる。
