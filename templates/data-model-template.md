# データモデル: [機能名]

> **段階**: ④ Plan の補助（`plan.md` §3 の詳細）
> **対応する設計**: `./plan.md`  |  **対応する仕様**: `./spec.md`
> **作成日**: [YYYY-MM-DD]
>
> `plan.md` の §3 データモデルが表1つに収まらないほど大きいときに、ここへ切り出す。
> エンティティ・属性・関係・制約・状態遷移を、実装に落とせる粒度で定義する。
> 小さな機能では plan.md の表だけで十分——このファイルは作らなくてよい。

---

## 1. エンティティ一覧

| エンティティ | 役割（何を表すか） | 対応する仕様 |
|--------------|--------------------|--------------|
| [...] | [...] | [US-x / AC-x.x] |

## 2. 各エンティティの定義

### [エンティティ名]

| 属性 | 型 | 必須 | 制約・既定値 | 説明 |
|------|----|----|--------------|------|
| [id] | [...] | ✓ | [一意・自動採番など] | [...] |
| [...] | [...] | | [...] | [...] |

> 制約は「なぜその制約か」を仕様の受け入れ基準と結びつけて書く。

## 3. 関係（リレーション）

> エンティティ間の関連と多重度（1対1 / 1対多 / 多対多）。

| 関係 | 多重度 | 説明・削除時の挙動 |
|------|--------|--------------------|
| [A] — [B] | [1対多] | [例 — 親削除で子もカスケード削除] |

```
[必要ならER図やASCIIで関係を図示]
```

## 4. 状態遷移（状態を持つエンティティのみ）

> 取りうる状態と、遷移のトリガー・条件。不正な遷移を禁じる根拠になる。

| 現在の状態 | イベント | 次の状態 | 条件 |
|------------|----------|----------|------|
| [...] | [...] | [...] | [...] |

## 5. 不変条件（Invariants）

- [常に成り立つべき条件。例 — 残高は負にならない]

> これらは実装時のバリデーションとテストの根拠になる。
